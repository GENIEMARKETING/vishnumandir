/**
 * Admin route group layout.
 * Intentionally minimal — no public site Header or Footer.
 */
export default function AdminLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
